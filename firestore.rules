rules_version = '2';
// All reads/writes go through the `api` Cloud Function (admin SDK bypasses these
// rules). Clients never touch Firestore directly, so deny everything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
